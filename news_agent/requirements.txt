feedparser==6.0.11
markdown>=3.5
python-dotenv==1.0.1
trafilatura==1.12.2
requests==2.32.3
beautifulsoup4==4.12.3
flask==3.1.0
lxml==5.3.0
openai>=1.30.0
APScheduler>=3.10
openpyxl>=3.1.0                 # Excel read/write for the openrouter_usage dashboard fetcher

# Browser automation for full-content fetching (requires Python 3.11+)
playwright>=1.40.0
browser-use>=0.1.0

# YouTube transcript feature
youtube-transcript-api>=0.6.0   # fast path: fetch captions without audio download
yt-dlp>=2024.1.0                # audio-only download fallback (requires ffmpeg on server)
pydub>=0.25.1                   # audio chunking on silence boundaries
dashscope>=1.20.0               # DashScope SDK - used for qwen3-asr-flash speech-to-text
pikepdf>=8.0.0                  # PDF post-compression - recompresses Playwright-generated PDFs without quality loss
# NOTE: pydub requires ffmpeg to be installed on the server:
#   Ubuntu/Debian: apt-get install ffmpeg
#   macOS:         brew install ffmpeg
